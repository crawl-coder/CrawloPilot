# CrawloPilot 前端页面评估报告

> 生成时间：2026-08-06
> 评估范围：`frontend/src` 全部页面、组件、API 层、路由、工具函数
> 技术栈：Vue 3 + Element Plus 2.5 + Vite 5 + Pinia + ECharts 5 + Monaco Editor

---

## 一、整体评估结论

前端**架构清晰、代码规范、功能完整**，已达到可交付水平。以下为详细评估。

### 做得好的方面 ✅

| 方面 | 说明 |
|------|------|
| 技术栈 | Vue 3 + Element Plus + Vite + Pinia + ECharts + Monaco，主流且现代化 |
| 代码规范 | 统一 Composition API + `<script setup>`，风格一致 |
| 可复用性 | 抽离公共组件（`StatCard`、`Pagination`）、composables（`useEcharts`、`usePagination`）、工具函数（`common.js`、`format.js`） |
| API 封装 | 按模块拆分 API 文件，axios 统一拦截器处理 token 与错误（401/403/404/500） |
| 权限控制 | 路由 meta 标记 + 导航守卫 + 按钮级权限（`checkIsAdmin`） |
| 用户体验 | loading 状态、空状态、错误提示、操作确认、相对时间格式化等细节到位 |
| 实时能力 | WebSocket 任务状态/日志推送（`/ws/tasks`），并有轮询降级方案 |

整体代码质量**高于一般内部系统水平**。

---

## 二、需优化的问题

### 🔴 高优先级（影响功能/正确性）

#### 问题 1：引入了 Pinia 但未使用，状态管理靠 localStorage hack

- **位置**：`frontend/src/main.js:36`（`app.use(createPinia())`）、`frontend/src/views/Layout.vue:445`
- **现象**：`src/` 下无 `store/` 目录，没有任何 Pinia store。登录用户信息存 `localStorage`，通过监听自定义 `storage` 事件跨组件同步——这是 hack 方案。
- **影响**：状态分散、不同步风险高、难以维护。
- **建议**：建立 `useUserStore` 集中管理登录态/用户信息/权限，组件统一从 store 读取，移除 localStorage 手动同步。

#### 问题 2：Token 过期用整页硬跳转

- **位置**：`frontend/src/api/request.js:34-38`
- **现象**：401 时 `setTimeout` 延迟后 `window.location.href = '/login'` 强制刷新页面。
- **影响**：整页刷新丢失 SPA 状态，体验生硬；用户编辑中数据会丢失。
- **建议**：改用 `router.push('/login')`，配合 store 清理状态。

#### 问题 3：系统菜单硬编码，无动态权限配置

- **位置**：`frontend/src/views/Layout.vue:233-250`
- **现象**：菜单项写死在数组，仅按 `requiresAdmin` 简单过滤。
- **影响**：无法按角色/权限点细粒度控制菜单；新增菜单需改代码。
- **建议**：后端返回用户菜单/权限点，前端动态渲染。

---

### 🟡 中优先级（影响可维护性/一致性）

#### 问题 4：无统一设计 Token，颜色/样式散落且风格不一致

- **位置**：多组件内联 hex 色值
- **现象**：`#5470c6`、`#F78166`、`#722ED1`、`#6e7681`（GitHub 深色）与 `#f0f2f5`、白底（浅色）**混用**；`Dashboard.vue` 用深色卡片（`#161b22`），而 Login 及其他页面为浅色——**视觉风格不统一**。
- **影响**：品牌感弱、后期改色困难、界面割裂。
- **建议**：建立 SCSS/CSS 变量或设计 Token（主色/成功/警告/危险/文字/背景），全局统一管理，杜绝魔法色值；统一深浅色风格。

#### 问题 5：状态/类型映射逻辑重复

- **位置**：`common.js` 已有 `TASK_STATUS_MAP`，但 `TaskDetail.vue`、`Nodes.vue`、`Users.vue` 仍各自定义 `getStatusType` 等。
- **影响**：同一状态多处定义，改动需同步多处，易漏。
- **建议**：状态映射全部收敛到 `common.js`，页面统一引用。

#### 问题 6：缺少错误边界与全局反馈

- **现象**：各页面 `catch` 仅 `ElMessage.error`，无统一异常上报；切换路由无进度条。
- **建议**：加路由切换进度条（如 nprogress）、全局错误捕获（`app.config.errorHandler`）。

#### 问题 7：表格分页参数一致性风险

- **现象**：`usePagination` 与部分页面手写 `handleCurrentChange` 并存，需确认与后端 `page/size` 契约一致。
- **建议**：统一分页参数约定，前后端对齐。

---

### 🟢 低优先级（体验/打磨）

#### 问题 8：移动端适配不足

- **现象**：`hide-on-mobile` 仅隐藏副标题，侧边栏小屏无折叠抽屉，表格横向溢出。
- **说明**：管理端可接受；如需移动端需专项处理。

#### 问题 9：可访问性（a11y）欠缺

- **现象**：图标按钮缺 `aria-label`，状态仅靠颜色区分（色弱不友好）。
- **建议**：补充 aria 属性，状态增加文字/图标双重标识。

#### 问题 10：首屏性能可优化

- **现象**：Element Plus 全量引入 + Monaco + ECharts，首屏 bundle 较大。
- **现状**：路由已懒加载（`() => import`）✅。
- **建议**：ECharts 按需引入、Monaco 仅编辑器页加载、考虑组件库按需引入。

---

## 三、问题汇总表

| # | 优先级 | 问题 | 类型 |
|---|--------|------|------|
| 1 | 🔴 高 | Pinia 引入未用，状态靠 localStorage hack | 架构 |
| 2 | 🔴 高 | Token 过期整页硬跳转 | 体验/正确性 |
| 3 | 🔴 高 | 菜单硬编码无动态权限 | 架构 |
| 4 | 🟡 中 | 无设计 Token，深浅色风格不统一 | 一致性 |
| 5 | 🟡 中 | 状态映射逻辑重复 | 可维护性 |
| 6 | 🟡 中 | 缺错误边界与全局反馈 | 可维护性 |
| 7 | 🟡 中 | 分页参数一致性风险 | 契约 |
| 8 | 🟢 低 | 移动端适配不足 | 体验 |
| 9 | 🟢 低 | 可访问性欠缺 | 体验 |
| 10 | 🟢 低 | 首屏性能可优化 | 性能 |

---

## 四、结论

以上均为**优化项而非阻塞 Bug**，不影响当前系统正常使用。建议优先处理 3 个高优先级问题（状态管理、跳转方式、动态菜单），再逐步收敛设计 Token 与重复逻辑。

---

## 五、修复进展（2026-08-06）

以下问题已修复并通过 `vite build` 构建验证（exitCode 0），无 lint 错误：

| # | 问题 | 修复状态 | 修复内容 |
|---|------|---------|---------|
| 1 | Pinia 未用，状态靠 localStorage hack | ✅ 已修复 | 新建 `src/stores/user.js`（`useUserStore`）集中管理 token/用户信息/登录态/权限；`Login.vue`、`Layout.vue` 改用 store |
| 2 | Token 过期整页硬跳转 | ✅ 已修复 | `request.js` 401 改用 `router.push('/login')`，避免整页刷新丢 SPA 状态 |
| 3 | 菜单硬编码无动态权限 | ✅ 已修复 | `Layout.vue` 系统管理菜单按 `userStore.isAdmin` 动态显隐；`router/index.js` 为 `/users` 加 `meta.requiresAdmin` 守卫，非 admin 重定向 `/dashboard` |
| 4 | 无设计 Token，深浅色不统一 | ✅ 已修复 | 新建 `src/styles/tokens.css` 定义统一 Token；`Layout.vue`、`Login.vue` 侧边栏/头部/主区/登录渐变改用 CSS 变量 |
| 5 | 状态映射逻辑重复 | ✅ 已修复 | `ProjectDetail.vue` 移除本地重复的 `getStatusType`/`getStatusText`/`formatDate`，改用 `common.js` 统一函数 |
| 6 | 缺错误边界与全局反馈 | ✅ 已修复 | `main.js` 注册 `app.config.errorHandler` 全局错误边界；`App.vue` 挂载 `RouteProgress` 路由进度条 |
| 7 | 分页参数一致性 | ✅ 已确认 | 后端统一 `skip/limit`，与 `usePagination` 输出一致，无问题 |
| 8-10 | 移动端/a11y/首屏性能 | ⬜ 暂缓 | 属打磨项，管理端可接受，后续按需处理 |

### 新增文件

- `frontend/src/stores/user.js` —— Pinia 用户状态管理 Store
- `frontend/src/styles/tokens.css` —— 全局设计 Token（CSS 变量）
- `frontend/src/components/RouteProgress.vue` —— 路由切换顶部进度条

### 审查阶段二次修正（5 处）

初次修复后经代码审查，发现并修正以下次生缺陷：

| 修正点 | 问题 | 修正方式 |
|--------|------|---------|
| 循环依赖 | `request.js → router → stores/user → api/auth → request.js` 静态环 | `request.js` 移除顶层 `@/router` 静态导入，401 回调内改**动态 import** 打破环 |
| 401 状态不同步 | 只清 localStorage，Pinia `token` ref 失真 | 401 时动态导入 store 同步 `logout()` |
| `fetchUser` 过度登出 | 网络/5xx 也清空登录态 | 改为**仅 401 时登出** |
| `Lock` 图标未注册（预先存在） | `Login.vue` 密码框图标不渲染 | 本地导入 `User`/`Lock` 并改组件绑定 |
| 重复请求 | 守卫已拉取用户，Layout 挂载又请求 | 加 `userStore.user` 缓存判断 |

### 发现的后端权限点

- 后端 `GET /users` 接口对**非 admin 用户返回 200**（普通用户 roles 为空时仍可访问）。前端已通过 `requiresAdmin` 守卫拦截，但**建议后端在用户模块接口补充 admin 权限校验**作为第二道防线。
